syntax = "proto3";

// A text from entities are to be extracted, including language metadata.
message Text {
    // The text snippet.
    string text = 1;

    // An array of possible languages.
    repeated string languages = 2;

    // An array of possible countries.
    repeated string countries = 3;
}
