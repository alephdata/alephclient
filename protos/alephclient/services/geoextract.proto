syntax = "proto3";

import "alephclient/services/common.proto";

// Try to guess to country of a given set of texts.
service GeoExtract {
    // Return a set of candidate countries.
    rpc ExtractCountries (stream Text) returns (CountryTags);
}

message CountryTags {
    repeated string countries = 1;
}
